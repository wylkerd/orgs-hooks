import produtores from '../mocks/produtores';
import topo from '../mocks/topo';

// ARQUIVO PARA SIMULAÇÃO DO CONSUMO DE DADOS DE UMA API EXTERNA
export const carregaProdutores = () => {
  return produtores
}

export const carregaTopo = () => {
  return topo
}